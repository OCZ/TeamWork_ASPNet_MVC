<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("ABRUPLE APP", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" }))
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @if (Request.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Profile", "Index", "Home")</li>
                    <li>@Html.ActionLink("Manage Own Contests", "Contact", "Home")</li>
                }
                <li>@Html.ActionLink("Active Contests", "Index", "Home")</li>
                <li>@Html.ActionLink("Past Contests", "Contact", "Home")</li>
                @if (this.User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("To Administration Panel", "Index", "Home", routeValues: new { area = "Admin" }, htmlAttributes: null)</li>

                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>
